# Eureka-ServiceRegistry-Microservices
Eureka server acts as service registry that holds information about all available microservies instances. like here holds only employee and department service data. it enables automatic registration of services and simplifies inter-service communication without hardcoding IP addresses or hostnames.
